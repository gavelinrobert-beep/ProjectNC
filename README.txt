See main instructions in chat. Run: docker compose up --build, then http://localhost:5173 and http://localhost:8000/docs


## Snabbstart
```bash
cd Aegis
cp .env.sample .env
docker compose up --build
```
